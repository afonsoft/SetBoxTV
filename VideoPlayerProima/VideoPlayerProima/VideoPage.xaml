<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SetBoxTV.VideoPlayer.Model"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="DarkGray"
             xmlns:vlc="clr-namespace:LibVLCSharp.Forms.Shared;assembly=LibVLCSharp.Forms"
             x:Class="SetBoxTV.VideoPlayer.VideoPage">
    <ContentPage.BindingContext>
        <local:VideoViewModel />
    </ContentPage.BindingContext>

    <StackLayout x:Name="PageStackLayout">
        <StackLayout.GestureRecognizers>
            <TapGestureRecognizer Tapped="OnTapped" NumberOfTapsRequired="2" />
        </StackLayout.GestureRecognizers>

        <vlc:VideoView x:Name="videoPlayer" 
                       HorizontalOptions="FillAndExpand" 
                       VerticalOptions="FillAndExpand" 
                       IsVisible="False"/>

        <Image x:Name="imagePlayer" BackgroundColor="Black" Aspect="Fill" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" >
            <Image.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnTapped" NumberOfTapsRequired="2" />
            </Image.GestureRecognizers>
        </Image>
    </StackLayout>
</ContentPage>