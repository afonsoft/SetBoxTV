<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SetBoxTV.VideoPlayer.Model"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="DarkGray"
             xmlns:vlc="clr-namespace:LibVLCSharp.Forms.Shared;assembly=LibVLCSharp.Forms"
             x:Class="SetBoxTV.VideoPlayer.VideoPage">
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.GestureRecognizers>
            <TapGestureRecognizer NumberOfTapsRequired="2" Tapped="OnTapped" />
        </Grid.GestureRecognizers>
        <vlc:VideoView x:Name="VideoView" Grid.Row="0" Grid.Column="0" MediaPlayerChanged="MediaPlayerChanged"/>
    </Grid>

</ContentPage>